apiVersion: v1
kind: Service
metadata:
  name: bloodnet-service
  labels:
    app: bloodnet   
spec:
  type: NodePort
  selector:
    app: bloodnet
  ports:
    - name: http
      port: 80
      targetPort: 5000